{
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"lerna": "^3.16.4",
		"vsce": "^1.71.0"
	},
	"scripts": {
		"build": "lerna run build",
		"package": "mkdir -p build && rm -rf build/* && lerna run package",
		"test": "lerna run test",
		"watch": "lerna run watch"
	}
}
